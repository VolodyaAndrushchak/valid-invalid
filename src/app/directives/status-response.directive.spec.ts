import { StatusResponseDirective } from './status-response.directive';

describe('StatusResponseDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusResponseDirective();
    expect(directive).toBeTruthy();
  });
});
